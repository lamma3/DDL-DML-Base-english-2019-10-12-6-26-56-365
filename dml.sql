-- Insert record
insert into student (age) values (1);
-- Revise record
select * from student;
--  Delete record
delete from student where age = 1;
-- Search record
select * from student where age = 1;
